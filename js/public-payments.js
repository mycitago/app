(function(global){
  const $=id=>document.getElementById(id);
  let ctx=null;
  function money(n){return new Intl.NumberFormat('es-MX',{style:'currency',currency:'MXN'}).format(Number(n||0))}
  function estimatedAmount(service,options){const price=Number(service?.price||0),mode=options?.collection_mode,value=Number(options?.deposit_value||0);if(mode==='full')return price;if(mode==='fixed')return Math.min(price,value);if(mode==='percentage')return Math.min(price,price*Math.min(100,value)/100);return 0}
  async function loadOptions(businessId){const{data,error}=await supabaseClient.rpc('get_public_payment_options',{p_business_id:businessId});if(error)throw error;return data||{enabled:false,ready:false,provider:'mercadopago',collection_mode:'none'}}
  async function checkout(){const btn=$('btn-pay-online');if(!ctx||!btn)return;try{btn.disabled=true;btn.textContent='Abriendo Mercado Pago…';const{data,error}=await supabaseClient.functions.invoke('mp-customer-checkout',{body:{appointment_id:ctx.appointmentId,access_token:ctx.accessToken}});if(error)throw error;if(!data?.ok)throw new Error(data?.error||'No se pudo iniciar el pago');if(data?.already_paid){btn.textContent='Pago recibido';btn.disabled=true;const copy=$('online-payment-copy');if(copy)copy.textContent='Este pago ya fue confirmado.';return}if(!data?.url)throw new Error('Mercado Pago no devolvió una liga de pago');location.href=data.url}catch(e){console.error('[MyCitaGo Mercado Pago checkout]',e);btn.disabled=false;btn.textContent=ctx.buttonLabel;if(typeof showToast==='function')showToast('La cita sigue confirmada, pero no pudimos abrir Mercado Pago. Puedes intentarlo de nuevo.')}}
  async function prepare(payload){ctx=payload;const card=$('online-payment-card'),btn=$('btn-pay-online'),copy=$('online-payment-copy');if(!card||!btn||!payload?.business?.id||!payload?.appointmentId||!payload?.accessToken)return;try{const options=await loadOptions(payload.business.id);if(!options.enabled||!options.ready||options.provider!=='mercadopago'||options.collection_mode==='none'){card.hidden=true;return}const amount=estimatedAmount(payload.service,options);if(!(amount>0)){card.hidden=true;return}const full=options.collection_mode==='full';ctx.buttonLabel=full?`Pagar ${money(amount)}`:`Pagar anticipo ${money(amount)}`;btn.textContent=ctx.buttonLabel;copy.textContent=full?'Paga de forma segura con Mercado Pago. Tu cita ya quedó registrada.':'Puedes pagar el anticipo de forma segura con Mercado Pago. Tu cita ya quedó registrada.';btn.onclick=checkout;card.hidden=false}catch(e){console.warn('[MyCitaGo payment options]',e);card.hidden=true}}
  function showReturnMessage(){const status=new URLSearchParams(location.search).get('payment');if(!status)return;const messages={approved:'Mercado Pago aprobó el pago. La cita permanece confirmada.',pending:'El pago quedó pendiente en Mercado Pago. La cita permanece registrada.',failure:'El pago no se completó. Tu cita permanece registrada y puedes intentar pagar nuevamente.'};const message=messages[status];if(message)setTimeout(()=>{if(typeof showToast==='function')showToast(message)},100)}
  document.addEventListener('DOMContentLoaded',showReturnMessage);
  global.CitagoPublicPayments={prepare};
})(window);
