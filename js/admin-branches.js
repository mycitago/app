let branchBiz=null,branches=[];const R=id=>document.getElementById(id);const esc=s=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;'}[c]));function rToast(m){const e=R('toast');e.textContent=m;e.classList.remove('hidden');setTimeout(()=>e.classList.add('hidden'),2500)}async function loadBranches(){const {data,error}=await supabaseClient.from('business_branches').select('*').eq('business_id',branchBiz.id).order('is_primary',{ascending:false}).order('name');if(error)throw error;branches=data||[];renderBranches()}function renderBranches(){R('branches').innerHTML=branches.map(b=>`<article class="branch-card"><div><span class="branch-icon">⌂</span><div><strong>${esc(b.name)}</strong><small>${b.is_primary?'Sucursal principal · ':''}${b.active?'Activa':'Inactiva'}</small></div></div><p>${esc(b.address||'Sin dirección capturada')}</p><p>${esc(b.phone||'Sin teléfono')}</p><button data-edit-branch="${b.id}">Editar</button></article>`).join('')||'<div class="team-empty">No hay sucursales. Ejecuta la migración para crear la principal.</div>';R('branches').querySelectorAll('[data-edit-branch]').forEach(x=>x.onclick=()=>openBranch(x.dataset.editBranch))}function openBranch(id=null){const b=branches.find(x=>x.id===id);R('branch-id').value=b?.id||'';R('branch-name').value=b?.name||'';R('branch-phone').value=b?.phone||'';R('branch-address').value=b?.address||'';R('branch-timezone').value=b?.timezone||'America/Mexico_City';R('branch-active').checked=b?.active!==false;R('branch-title').textContent=b?'Editar sucursal':'Nueva sucursal';R('branch-dialog').classList.remove('hidden')}function closeBranch(){R('branch-dialog').classList.add('hidden')}async function saveBranch(e){e.preventDefault();const id=R('branch-id').value,payload={business_id:branchBiz.id,name:R('branch-name').value.trim(),phone:R('branch-phone').value.trim()||null,address:R('branch-address').value.trim()||null,timezone:R('branch-timezone').value,active:R('branch-active').checked,updated_at:new Date().toISOString()};if(!payload.name)return rToast('Escribe el nombre.');if(id){const {error}=await supabaseClient.from('business_branches').update(payload).eq('id',id).eq('business_id',branchBiz.id);if(error)throw error}else{const {error}=await supabaseClient.from('business_branches').insert({...payload,is_primary:branches.length===0});if(error)throw error}closeBranch();rToast('Sucursal guardada');await loadBranches()}async function initBranches(){const s=await requireAuth();if(!s)return;branchBiz=await getMyBusiness(s.user);if(!branchBiz)return;R('new-branch').onclick=()=>openBranch();R('branch-form').onsubmit=e=>saveBranch(e).catch(err=>rToast(err.message));document.querySelectorAll('[data-branch-close]').forEach(x=>x.onclick=closeBranch);try{await loadBranches()}catch(e){R('branches').innerHTML='<div class="team-empty"><b>Activa Sucursales</b><span>Ejecuta SUPABASE_V6_MASTER.sql y recarga.</span></div>'}}
document.addEventListener('DOMContentLoaded',initBranches);
