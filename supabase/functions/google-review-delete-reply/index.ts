import { cors, context, getAccessToken, json } from "../_shared/google.ts";
Deno.serve(async req=>{if(req.method==="OPTIONS")return new Response("ok",{headers:cors});try{const c=await context(req),token=await getAccessToken(c.admin,c.business_id),review_id=String(c.body.review_id||"");if(!review_id)return json({error:"review_id requerido"},400);const {data:conn}=await c.admin.from("business_google_connections").select("google_account_id,location_id").eq("business_id",c.business_id).single();const endpoint=`https://mybusiness.googleapis.com/v4/${conn.google_account_id}/${conn.location_id}/reviews/${review_id}/reply`;const r=await fetch(endpoint,{method:"DELETE",headers:{Authorization:`Bearer ${token}`}});if(!r.ok)return json({error:await r.text()},502);await c.admin.from("business_google_reviews").update({reply_comment:null,reply_update_time:null}).eq("business_id",c.business_id).eq("review_id",review_id);return json({business_id:c.business_id,ok:true})}catch(e){return json({error:String(e.message||e)},400)}});
